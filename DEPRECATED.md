# DEPRECATED

This epic is complete and merged to main at v1.0.1 (2026-02-20).

Do not base new work on this branch.

**Milestone commit:** `milestone: v1.0.1 - Merge Policies with Checks`

**What changed:**
- Unified `policies/` and `checks/` → single `checks/` structure
- Both .md (interpretive) and .sh (deterministic) side-by-side
- 16 deterministic checks + 20 interpretive checks
- Removed redundant files, standardized naming
- Epic progress visible in backstage report
- Merge workflow: rebase → checks → merge → checks → push

See CHANGELOG for full details.
