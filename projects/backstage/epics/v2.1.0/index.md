# v2.1.0 - Backstage GUI

## Goal

Read-only visual interface for backstage filesystem (projects/epics/tasks).

## Philosophy

**GUI = always read-only**
- Changes via conversational AI (respects checks)
- GUI surfaces foundation (doesn't edit directly)
- Visualization → conversation → validated change

## Key Questions

**Mermaid as wireframe contract:**
- How to ensure unambiguous?
- Contract = implementation guide?
- Validation before coding?

## Status

Roadmap (awaiting v2.0.0 completion).
